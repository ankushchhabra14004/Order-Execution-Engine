{
  "name": "order-execution-engine",
  "version": "1.0.0",
  "description": "Mock order execution engine for Solana DEX with BullMQ queue, PostgreSQL, and Redis",
  "main": "server.js",
  "scripts": {
    "server": "node server.js",
    "worker": "node worker.js",
    "dev": "concurrently \"npm run server\" \"npm run worker\"",
    "test": "jest --runInBand",
    "demo": "node demo.js",
    "standalone": "node server-standalone.js"
  },
  "dependencies": {
    "bullmq": "^5.0.0",
    "dotenv": "^17.2.3",
    "ioredis": "^5.3.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "jest": "^29.7.0"
  }
}
